# On first setup of the repo, copy this example file, change the copy's name to "secret_config.yml" and put it in the same folder as "secret_config.yml-example"

JIRA_USERNAME:
JIRA_PASSWORD:
JIRA_URL:
